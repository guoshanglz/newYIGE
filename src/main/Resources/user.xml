<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="g">
    <select id="FindByid" resultType="com.springapp.mybatis.User" parameterType="string">
        SELECT * FROM login1 WHERE username=#{username}
    </select>
</mapper>

